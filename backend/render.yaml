services:
  - type: web
    name: laravel-backend
    env: docker
    region: oregon
    plan: free
    branch: main
    dockerfilePath: Dockerfile        # Esto está bien porque el Dockerfile está en "backend"
    dockerContext: .                  # Ahora el contexto debe ser la carpeta "backend"

    buildCommand: ""  # Ya está definido en el Dockerfile
    startCommand: ""  # Laravel servirá desde Apache, ya configurado

    envVars:
      - key: APP_ENV
        value: production

      - key: APP_KEY
        value: base64:TU_APP_KEY  # Reemplazar con tu APP_KEY real
